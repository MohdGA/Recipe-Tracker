<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Edit <%= foundMovie.title %></title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;600&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/stylesheet/edit.css">
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-custom">
    <a href="/" class="navbar-brand">MovieWatchList</a>
  </nav>
  <main>
    <h1>Edit <%= foundMovie.title %></h1>
    <form action="/movies/<%= foundMovie._id %>?_method=PUT" method="POST">

      <label for="title">Title</label>
      <input type="text" id="title" name="title" value="<%= foundMovie.title %>" required />

      <label for="description">Description</label>
      <textarea id="description" name="description" rows="4"><%= foundMovie.description %></textarea>

      <label for="price">Price</label>
      <input type="number" id="price" name="price"    value="<%= foundMovie.price %>" step="0.01" />

      <label for="isWatched">is Watched</label>
      <input type="checkbox" name="isWatched" id="isWatched">

      <img src="<%= foundMovie.image.url %>" alt="<%= foundMovie.title %>" />

      <button type="submit">Update Movie</button>
      
    </form>
    <a href="/movies" class="back-link">← Back to All Movies</a>
  </main>
  <!-- Bootstrap JS Bundle -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>


