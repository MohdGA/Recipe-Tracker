<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= foundMovie.title %></title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;600&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: url('https://images.unsplash.com/photo-1595769816263-9b910be24d5f?w=1920&auto=format&fit=crop&q=80&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxzZWFyY2h8NHx8bW92aWUlMjB0aGVhdGVyfGVufDB8fDB8fHww') no-repeat center center fixed;
      background-size: cover;
      min-height: 100vh;
      color: #F0E6D2;
      padding: 3rem 1rem 4rem;
      position: relative;
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    body::before {
      content: "";
      position: fixed;
      inset: 0;
      background: rgba(15, 15, 15, 0.8);
      z-index: -1;
    }
    nav.navbar-custom {
      background-color: rgba(20, 20, 20, 0.85);
      box-shadow: 0 2px 8px rgba(0,0,0,0.7);
      padding: 0.8rem 1.5rem;
      position: fixed;
      width: 100%;
      top: 0;
      z-index: 1030;
    }
    .navbar-brand {
      font-weight: 700;
      font-size: 1.6rem;
      color: #E1C699;
      text-decoration: none;
    }
    .navbar-brand:hover {
      color: #F2D6A9;
    }
    main.container {
      max-width: 700px;
      background: rgba(30, 30, 30, 0.9);
      border-radius: 15px;
      padding: 2rem 2.5rem;
      box-shadow: 0 8px 20px rgba(201, 167, 106, 0.7);
      margin-top: 90px;
      color: #F0E6D2;
      text-align: center;
    }
    main h1 {
      font-weight: 700;
      font-size: 2.5rem;
      margin-bottom: 1rem;
      color: #C9A76A;
      text-shadow: 0 0 10px rgba(201, 167, 106, 0.9);
    }
    main img {
      max-width: 100%;
      height: auto;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(201, 167, 106, 0.8);
      margin-bottom: 1.5rem;
    }
    main p {
      font-size: 1.1rem;
      margin: 0.7rem 0;
      color: #D8C9A3;
    }
    main p strong {
      color: #E1C699;
    }
    .actions {
      margin-top: 2rem;
      display: flex;
      justify-content: center;
      gap: 1.5rem;
      flex-wrap: wrap;
    }
    .actions form {
      margin: 0; /* Remove default form margin */
    }
    .actions form button,
    .actions a {
      background-color: #C9A76A;
      border: none;
      padding: 0.8rem 1.6rem; /* equal padding */
      border-radius: 30px;
      font-weight: 700;
      color: #2B1F00;
      cursor: pointer;
      text-decoration: none;
      box-shadow: 0 0 12px rgba(201, 167, 106, 0.9);
      transition: background-color 0.3s ease;
      display: inline-flex;       /* vertically center text */
      align-items: center;
      justify-content: center;
      min-width: 120px;
      text-align: center;
      font-size: 1rem;
      line-height: 1.2;
      user-select: none;
    }
    .actions form button:hover,
    .actions a:hover {
      background-color: #E1C699;
      color: #2B1F00;
      text-decoration: none;
    }
    a.back-link {
      margin-top: 1.5rem;
      color: #C9A76A;
      font-weight: 600;
      text-decoration: none;
      display: inline-block;
      transition: color 0.3s ease;
    }
    a.back-link:hover {
      color: #F2D6A9;
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <nav class="navbar-custom">
    <a href="/" class="navbar-brand">MovieWishList</a>
  </nav>
  <main class="container">
    <h1><%= foundMovie.title %></h1>
    <img src="<%= foundMovie.image.url %>" alt="<%= foundMovie.title %>" />
    <p><strong>Description:</strong> <%= foundMovie.description %></p>
    <p><strong>Price:</strong> $<%= foundMovie.price %></p>
    <p><strong>Phone:</strong> <%= foundMovie.phoneNumber %></p>
    <a href="/movies" class="back-link">&larr; Back to All Movies</a>
    <div class="actions">
      <form action="/movies/<%= foundMovie._id %>?_method=DELETE" method="POST" onsubmit="return confirm('Are you sure you want to delete this movie?');">
        <button type="submit">Delete</button>
      </form>
      <a href="/movies/<%= foundMovie._id %>/edit">Edit</a>
    </div>
  </main>
  <!-- Bootstrap JS Bundle -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>